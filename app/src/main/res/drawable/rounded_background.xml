<!-- rounded_checkbox_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/colorBlack" />
    <corners android:radius="20dp" /> <!-- Adjust the radius to control the roundness of the corners -->
    <stroke
        android:width="0.1dp"
        android:color="@color/colorBlack" /> <!-- Change the color and width of the border -->
</shape>
